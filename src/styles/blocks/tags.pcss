.tags {
  display: flex;
  flex-wrap: wrap;

  @include tablets {
    justify-content: center;
  }
}

.tags__item {
  margin-right: 8px;
  margin-bottom: 8px;
  
  &:last-child {
    margin-right: 0;
  }
}